# CodeRabbit configuration. Project guidance lives in AGENTS.md at the repository
# root, which CodeRabbit reads as review guidelines; this file only sets the review
# to comment rather than block, since the local review before commit is the gate.
language: en-GB
reviews:
  profile: chill
  request_changes_workflow: false
